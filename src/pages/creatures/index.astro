---
const response = await fetch('https://www.dnd5eapi.co/api/monsters');
const data = await response.json();
---
<ol>
  {data.results.map((item) => (<li><a href={`https://www.dnd5eapi.co${item.url}`}>{item.name}</a></li>))}
</ol>