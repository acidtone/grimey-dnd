---
import Stat from '../components/Stat.astro';

const { stats } = Astro.props;

// Why is this interface needed?
interface Armor {
  type: string,
  value: string
}
---

{stats.map((item: Armor) =>   
(<Stat title={`AC (${item.type})`} value={item.value} />)
)}

